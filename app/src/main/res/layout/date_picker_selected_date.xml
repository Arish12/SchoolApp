<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/day_picker_selected_date_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:background="@color/style_color_accent"
    android:gravity="center"
    android:orientation="vertical">

    <it.arisetech.app.arish.calendar.AccessibleLinearLayout
        android:id="@+id/date_picker_month_and_day"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:clickable="true"
        android:orientation="vertical"
        android:textColor="@color/blue_500" >

        <TextView
            android:id="@+id/date_picker_month"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:duplicateParentState="true"
            android:gravity="center_horizontal|bottom"
            android:includeFontPadding="false"
            android:textColor="#1100ffaa"
            android:textSize="25dp"
            tools:text="Baisakh"/>

        <TextView
            android:id="@+id/date_picker_day"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="-10dip"
            android:layout_marginTop="-10dip"
            android:duplicateParentState="true"
            android:gravity="center"
            android:includeFontPadding="false"
            android:textColor="#0055ff"
            android:textSize="40dp"
            tools:text="15"/>
    </it.arisetech.app.arish.calendar.AccessibleLinearLayout>

    <it.arisetech.app.arish.calendar.AccessibleTextView
        android:id="@+id/date_picker_year"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal|top"
        android:includeFontPadding="false"
        android:textColor="#c4ff00"
        android:textSize="25dp"
        tools:text="2073" />

</LinearLayout>